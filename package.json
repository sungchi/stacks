{
  "name": "garden-stacks-code",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:offline": "node scripts/build-offline.mjs",
    "build:current": "node scripts/build-offline.mjs --target hourly",
    "build:hourly": "node scripts/build-offline.mjs --target hourly",
    "build:legacy": "node scripts/build-offline.mjs --target legacy",
    "test": "node --test",
    "open": "open index.html",
    "serve": "python3 -m http.server 4173"
  }
}
